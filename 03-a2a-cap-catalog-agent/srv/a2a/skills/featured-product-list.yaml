# GENERATED by scripts/gen-skills.js — do not edit by hand.
# Re-run `npm run gen:skills` to regenerate. Hand-written meta-skills live
# alongside these files and are NOT touched by the generator.
id: featured-product-list
name: List Products
description: >-
  List Product records from API_PRODUCT_SRV/A_Product (first page, $top-capped). Returns a narrow,
  fixed set of fields. This is a curated example of the STATIC list pattern; for arbitrary
  services/entity sets or structured filters use the generic searchEntitySet meta-skill instead.
domain: master-data
tags:
  - product
  - material
  - master-data
  - list
  - featured
examples:
  - List products
  - Show the first 10 products
params:
  - name: top
    type: integer
    required: false
    min: 1
    max: 100
    default: 20
    description: Maximum number of rows to return (1-100).
backend:
  service: API_PRODUCT_SRV
  namespace: sap
  entitySet: A_Product
  operation: list
  select:
    - Product
    - ProductType
    - CrossPlantStatus
    - CrossPlantStatusValidityDate
    - CreationDate
    - CreatedByUser
    - LastChangeDate
    - LastChangedByUser
    - LastChangeDateTime
    - IsMarkedForDeletion
  top:
    param: top
    default: 20
    max: 100
