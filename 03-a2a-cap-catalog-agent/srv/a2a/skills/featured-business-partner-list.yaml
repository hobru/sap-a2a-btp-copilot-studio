# GENERATED by scripts/gen-skills.js — do not edit by hand.
# Re-run `npm run gen:skills` to regenerate. Hand-written meta-skills live
# alongside these files and are NOT touched by the generator.
id: featured-business-partner-list
name: List Business Partners
description: >-
  List Business Partner records from API_BUSINESS_PARTNER/A_BusinessPartner (first page,
  $top-capped). Returns a narrow, fixed set of fields. This is a curated example of the STATIC list
  pattern; for arbitrary services/entity sets or structured filters use the generic searchEntitySet
  meta-skill instead.
domain: master-data
tags:
  - business-partner
  - master-data
  - customer
  - supplier
  - list
  - featured
examples:
  - List business partners
  - Show the first 10 business partners
params:
  - name: top
    type: integer
    required: false
    min: 1
    max: 100
    default: 20
    description: Maximum number of rows to return (1-100).
backend:
  service: API_BUSINESS_PARTNER
  namespace: sap
  entitySet: A_BusinessPartner
  operation: list
  select:
    - BusinessPartner
    - Customer
    - Supplier
    - AcademicTitle
    - AuthorizationGroup
    - BusinessPartnerCategory
    - BusinessPartnerFullName
    - BusinessPartnerGrouping
    - BusinessPartnerName
    - BusinessPartnerUUID
  top:
    param: top
    default: 20
    max: 100
