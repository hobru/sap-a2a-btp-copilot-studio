# GENERATED by scripts/gen-skills.js — do not edit by hand.
# Re-run `npm run gen:skills` to regenerate. Hand-written meta-skills live
# alongside these files and are NOT touched by the generator.
id: featured-sales-order-list
name: List Sales Orders
description: >-
  List Sales Order records from API_SALES_ORDER_SRV/A_SalesOrder (first page, $top-capped). Returns
  a narrow, fixed set of fields. This is a curated example of the STATIC list pattern; for arbitrary
  services/entity sets or structured filters use the generic searchEntitySet meta-skill instead.
domain: sales
tags:
  - sales-order
  - sales
  - order
  - list
  - featured
examples:
  - List sales orders
  - Show the first 10 sales orders
params:
  - name: top
    type: integer
    required: false
    min: 1
    max: 100
    default: 20
    description: Maximum number of rows to return (1-100).
backend:
  service: API_SALES_ORDER_SRV
  namespace: sap
  entitySet: A_SalesOrder
  operation: list
  select:
    - SalesOrder
    - SalesOrderType
    - SalesOrganization
    - DistributionChannel
    - OrganizationDivision
    - SalesGroup
    - SalesOffice
    - SalesDistrict
    - SoldToParty
    - CreationDate
  top:
    param: top
    default: 20
    max: 100
