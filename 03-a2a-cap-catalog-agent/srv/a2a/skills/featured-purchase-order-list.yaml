# GENERATED by scripts/gen-skills.js — do not edit by hand.
# Re-run `npm run gen:skills` to regenerate. Hand-written meta-skills live
# alongside these files and are NOT touched by the generator.
id: featured-purchase-order-list
name: List Purchase Orders
description: >-
  List Purchase Order records from API_PURCHASEORDER_PROCESS_SRV/A_PurchaseOrder (first page,
  $top-capped). Returns a narrow, fixed set of fields. This is a curated example of the STATIC list
  pattern; for arbitrary services/entity sets or structured filters use the generic searchEntitySet
  meta-skill instead.
domain: procurement
tags:
  - purchase-order
  - procurement
  - order
  - list
  - featured
examples:
  - List purchase orders
  - Show the first 10 purchase orders
params:
  - name: top
    type: integer
    required: false
    min: 1
    max: 100
    default: 20
    description: Maximum number of rows to return (1-100).
backend:
  service: API_PURCHASEORDER_PROCESS_SRV
  namespace: sap
  entitySet: A_PurchaseOrder
  operation: list
  select:
    - PurchaseOrder
    - CompanyCode
    - PurchaseOrderType
    - PurchasingDocumentDeletionCode
    - PurchasingProcessingStatus
    - CreatedByUser
    - CreationDate
    - LastChangeDateTime
    - Supplier
    - PurchaseOrderSubtype
  top:
    param: top
    default: 20
    max: 100
